# Plan: 01-01: Implement Service Container

Implement a lightweight, PSR-11 compliant Service Container with autowiring to support dependency injection across the system.

## Phase Boundary
**Phase:** 1 (Foundation)
**Plan:** 01-01
**Requirements:** REQ-1.1

## Proposed Changes

### Core

#### [NEW] [Container.php](file:///c:/Users/TESS%20LARON/Desktop/REVISED/src/Core/Container.php)
- Implement `get(string $id)`, `has(string $id)`.
- Implement `bind(string $abstract, $concrete = null)`.
- Implement `singleton(string $abstract, $concrete = null)`.
- Implement `resolve(string $abstract)` with `ReflectionClass` for autowiring.

#### [NEW] [App.php](file:///c:/Users/TESS%20LARON/Desktop/REVISED/src/Core/App.php)
- Implement a static `App` class to hold the global `Container` instance.
- Provide `App::container()` and `App::setContainer(Container $container)`.

## Verification Plan

### Automated Tests
- **ContainerTest:**
    - Verify that `singleton` returns the same instance.
    - Verify that `bind` (factory) returns a new instance.
    - Verify that nested dependencies are automatically resolved via autowiring.
    - Verify that `NotFoundException` is thrown for unregistered, non-autowirable keys.

### Manual Verification
- None (purely unit-tested logic).

## Tasks
```xml
<task description="Create src/Core/Container.php with PSR-11 compliance" status="todo">
    <read_first>src/Core/Router.php</read_first>
    <action>Implement the Container class with bind, singleton, and resolve methods.</action>
    <acceptance_criteria>src/Core/Container.php exists and implements PSR-11 (or equivalent interface).</acceptance_criteria>
</task>
<task description="Implement Autowiring using Reflection" status="todo">
    <read_first>src/Core/Container.php</read_first>
    <action>Add recursive resolution logic to the resolve method.</action>
    <acceptance_criteria>Container can resolve a class by injecting its constructor dependencies.</acceptance_criteria>
</task>
<task description="Create App bootstrap wrapper" status="todo">
    <action>Create src/Core/App.php for global container access.</action>
    <acceptance_criteria>App::container() returns the initialized container instance.</acceptance_criteria>
</task>
```

---
*Plan generated: 2026-04-09*
