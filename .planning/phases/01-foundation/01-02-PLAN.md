# Plan: 01-02: Database Refactor

Refactor the `Database` class from a static-only implementation to an instance-based model, using a Bridge pattern to maintain backward compatibility.

## Phase Boundary
**Phase:** 1 (Foundation)
**Plan:** 01-02
**Requirements:** REQ-3.1, REQ-2.3

## Proposed Changes

### Core

#### [MODIFY] [Database.php](file:///c:/Users/TESS%20LARON/Desktop/REVISED/src/Core/Database.php)
- Add instance properties: `connection`, `transactionDepth`.
- Convert `connect()`, `query()`, `beginTransaction()`, etc., to instance methods.
- Update the constructor to take `config` as an array.
- **Implement Bridge:** Re-implement static methods (`query`, `transaction`, etc.) to resolve the `Database` instance from `App::container()` and delegate to instance methods.

## Verification Plan

### Automated Tests
- **DatabaseTest:** (New/Existing)
    - Verify `App::container()->get(Database::class)` returns a working instance.
    - Verify that `Database::query()` (static) still performs queries correctly.
    - Verify nested transactions (savepoints) work correctly via the instance state.

### Manual Verification
- Load the application dashboard to ensure the Bridge correctly handles the existing heavy queries.

## Tasks
```xml
<task description="Refactor Database properties and constructor" status="todo">
    <read_first>src/Core/Database.php</read_first>
    <action>Change static properties to private instance properties. Add public __construct(array $config).</action>
    <acceptance_criteria>Database class has no static properties (except the Bridge-related ones if needed).</acceptance_criteria>
</task>
<task description="Convert Database methods to instance methods" status="todo">
    <read_first>src/Core/Database.php</read_first>
    <action>Remove 'static' from query, fetch, transaction, etc.</action>
    <acceptance_criteria>Database methods utilize $this->connection and $this->transactionDepth.</acceptance_criteria>
</task>
<task description="Implement the static Bridge" status="todo">
    <read_first>src/Core/Database.php</read_first>
    <action>Re-add static wrappers that call App::container()->get(self::class)->method().</action>
    <acceptance_criteria>Database::query() works without breaking current calling code.</acceptance_criteria>
</task>
```

---
*Plan generated: 2026-04-09*
